// Add Numbers
const add = (x,y) => x + y;

// Product Numbers
const product = (x,y) => x * y;
// exports.product = (x,y) => x * y;

module.exports = {add, product};  // module exports is an object that contains all the named exports of a module that exports the exported function

// export default {add, product};