// Add Numbers
const add = (x,y) => x + y;

// Product Numbers
const product = (x,y) => x * y;
// exports.product = (x,y) => x * y;

module.exports = {add, product};

// export default {add, product};